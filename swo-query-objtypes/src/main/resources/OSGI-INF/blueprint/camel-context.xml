<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
	xmlns:camel="http://camel.apache.org/schema/blueprint" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
       http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

	<camelContext trace="false" id="swo-query-objtypes-context" xmlns="http://camel.apache.org/schema/blueprint">
	    <route id="swo-query-objtypes-route">
	    	<from uri="timer://init?repeatCount=1"/>
	    	<to uri="bean:createSwoQueryObjtypesRequest"/>
	        <to uri="file:work/swo-query-objtypes?fileName=request-${date:now:yyyy-MM-dd-'T'-hh-mm-ss.SSS}.xml"/>
	        <log message="BEGIN: swo-query-objtypes-route ${date:now:yyyy-MM-dd-'T'-hh:mm:ss.SSS}" loggingLevel="INFO"/>
	        <to uri="sap-srfc-destination:quickstartDest:SWO_QUERY_OBJTYPES"/>
	        <log message="END: swo-query-objtypes-route ${date:now:yyyy-MM-dd-'T'-hh:mm:ss.SSS}" loggingLevel="INFO"/>
	        <to uri="file:work/swo-query-objtypes?fileName=response-${date:now:yyyy-MM-dd-'T'-hh-mm-ss.SSS}.xml"/>
	    </route>
	</camelContext>

	<bean id="createSwoQueryObjtypesRequest" class="org.jboss.fuse.swo.query.objtypes.CreateSwoQueryObjtypesRequest"/>

	<!-- Configures the Inbound and Outbound SAP Connections -->
	<bean id="sap-configuration"
		class="org.fusesource.camel.component.sap.SapConnectionConfiguration">
		<property name="destinationDataStore">
			<map>
				<entry key="quickstartDest" value-ref="quickstartDestinationData" />
			</map>
		</property>
	</bean>

	<!-- Configures an Outbound SAP Connection -->
	<!-- *** Please enter the connection property values for your environment 
		*** -->
	<bean id="quickstartDestinationData"
		class="org.fusesource.camel.component.sap.model.rfc.impl.DestinationDataImpl">
		<property name="ashost" value="${jco.client.ashost}" />
		<property name="sysnr" value="${jco.client.sysnr}" />
		<property name="client" value="${jco.client.client}" />
		<property name="user" value="${jco.client.user}" />
		<property name="passwd" value="${jco.client.passwd}" />
		<property name="lang" value="${jco.client.lang}" />
	</bean>

</blueprint>
